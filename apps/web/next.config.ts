import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['uoft.me', 'utsc.utoronto.ca'], // Add university domains here
  },
};

export default nextConfig;
