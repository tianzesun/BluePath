# BluePath Prompt Library

### Prompt: Initialize Neo4j Schema
> "Based on the DATA_MODEL.md, write a Python script for the FastAPI backend that initializes the Neo4j database with constraints for Course codes and basic indices for faster pathfinding."

### Prompt: Build Graph Component
> "Using React Flow and Tailwind, create a 'CourseNode' component in Next.js. It should display the course code, title, and have a color-coded border based on the department property in the JSON."

### Prompt: AI Ingestion Setup
> "Write a Python service in /packages/ingestion that takes a raw text string from a course syllabus and uses an LLM to extract prerequisites into a structured JSON object matching our Neo4j schema."
